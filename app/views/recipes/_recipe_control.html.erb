<% if logged_in? && authorize?(recipe.user) %>
  <%= link_to 'Editar', edit_recipe_path(recipe), class:"btn btn-warning" %>
  <%= link_to 'Deletar', recipe_path(recipe), method: :delete, class:"btn btn-danger" %>
<% end %>
