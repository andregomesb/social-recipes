<% if logged_in? && authorize?(recipe.user) %>
<%= link_to 'Editar', edit_recipe_path(recipe), class:"btn btn-warning btn-xs" %>
<%= link_to 'Deletar', recipe_path(recipe), method: :delete, class:"btn btn-danger btn-xs" %>
<% end %>
