<% if logged_in? && authorize?(recipe.user) %>
<%= link_to t('button.edit'), edit_recipe_path(recipe), class:"btn btn-warning btn-xs" %>
<%= link_to t('button.delete'), recipe_path(recipe), method: :delete, class:"btn btn-danger btn-xs" %>
<% end %>
