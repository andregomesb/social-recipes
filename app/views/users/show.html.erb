<div class="row">
  <aside class="col-md-4">
    <section class="user_info">
      <h1>
        <% if current_user == @user %>
          <%=  "Bem-vindo #{current_user.name}" %>
        <% else %>
          <%= @user.name %>
        <%end %>
      </h1>
    </section>
  </aside>
</div>
